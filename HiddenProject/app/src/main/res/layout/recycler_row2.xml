<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:background="@drawable/underline_background">

    <!-- ImageView (Kullanıcı profil resmi) -->
    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/imageView"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:contentDescription="selectImage"
        android:padding="5dp"
        app:shapeAppearanceOverlay="@style/circular"
        app:srcCompat="@drawable/profile"
        />

    <!-- Dikey Layout (Kullanıcı adı ve yorum içeriği) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_vertical">

        <!-- Kullanıcı Adı -->
        <TextView
            android:id="@+id/usernameTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Hidden Kullanıcısı"
            android:textStyle="bold"
            android:textSize="16dp"
            android:layout_marginStart="8dp" />

        <!-- Yorum İçeriği -->
        <TextView
            android:id="@+id/textViewCommentContent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Yorum İçeriği"
            android:layout_marginStart="8dp"
            android:layout_marginTop="4dp"/>
    </LinearLayout>
</LinearLayout>
